using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Token : MonoBehaviour
{ 
    [Tooltip("For every element wich define the level's complexity including TP's tokens.\nFrom 0 onwards.")]
    public int tokenOrder;
}
